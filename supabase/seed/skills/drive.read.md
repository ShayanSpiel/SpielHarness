# Google Drive Read

Read one Google Drive file by ID without changing it.

## Input

JSON with `fileId`. Preserve the file name, MIME type, modified date, source link, and the retrieved content in evidence.
