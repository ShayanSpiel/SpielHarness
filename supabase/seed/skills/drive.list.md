# Google Drive List

List recent, non-deleted files from the connected Google Drive account.

## Input

Optional JSON with a `query` string. Keep results read-only and record file IDs, names, types, modified dates, and source links.
