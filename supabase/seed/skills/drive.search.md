# Google Drive Search

Search file names and indexed file content in the connected Google Drive account.

## Input

JSON with a concise `query` string. Return file IDs and provenance so relevant files can be read with `drive.read`.
