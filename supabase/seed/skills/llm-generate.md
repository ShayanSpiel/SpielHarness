# LLM Generate

Calls the configured chat model to produce text from the active role prompt, node prompt, attached files, and user request.

## Free/local use
Works with the configured provider. If no provider key is configured, the app should surface a clear connection message instead of pretending generation happened.

## Input
- role prompt
- node prompt
- user request
- attached knowledge

## Output
Generated markdown text.
