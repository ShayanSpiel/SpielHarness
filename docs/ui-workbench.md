# Run Workbench

The workbench is the primary UX surface. It pairs a chat-like run input with visible marketing-team machinery:

- prompt input
- artifact attachment area
- selected roles
- live event stream
- right-side inspector
- generated artifact viewer

The current vertical slice supports eval-only runs. Next slices should preserve the same event and artifact contracts while adding content, research, ads, and publish graph templates.
