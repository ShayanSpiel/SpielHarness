// Re-exported from the shared providers package for convenience.
// The canonical implementation lives in packages/providers/src/http/auth.ts.
export {
  encryptConnectionSecret,
  decryptConnectionSecret,
} from "@spielos/providers";
